# OpenAI API Key (Chatbot에서 사용)
OPENAI_API_KEY=your_openai_api_key_here

# Spring API Host (Crawler에서 사용)
# Docker 환경: host.docker.internal (기본값)
# 외부 서버: 실제 IP 또는 도메인
SPRING_API_HOST=host.docker.internal
