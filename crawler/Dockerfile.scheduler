# Scheduler Dockerfile (크롤링 스케줄러)
FROM python:3.12-slim

# 작업 디렉토리 설정
WORKDIR /app

# 필요한 패키지만 설치 (requests, apscheduler)
COPY requirements.txt .
RUN pip install --no-cache-dir requests apscheduler

# 스케줄러 스크립트 복사
COPY crawler/scheduler.py .

# 스케줄러 실행
CMD ["python", "scheduler.py"]
